import 'package:flutter_minimeetup/src/components/api_provider.dart';
import '../models/models.dart';

/*class Repository {
  final ApiProvider _provider;

  Repository(this._provider);

  Stream<ColorState> colorState(ColorEvent event) => _provider.getColorState(event: event);

  void changeColorState(ColorEvent event) => _provider.changeColor(event: event);

  Stream<int> counter() => _provider.counter();
}*/
