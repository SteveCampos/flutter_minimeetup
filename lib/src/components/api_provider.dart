import '../models/models.dart';

/*class ApiProvider {
  Stream<ColorState> getColorState({ColorEvent event}) async* {
    if (event == null) {
      event = ColorEvent.changeToBlue;
    }

    switch (event) {
      case ColorEvent.changeToBlue:
        yield ColorState.blue;
        break;
      case ColorEvent.changeToRed:
        yield ColorState.red;
        break;
      case ColorEvent.changeToYellow:
        yield ColorState.yellow;
        break;
    }
  }

  Stream<int> counter() async* {
    for (int i = 0; i <= 1000; i++) {
      yield i;
    }
  }

  changeColor({ColorEvent event}) {
    getColorState(event: event);
  }
}*/
